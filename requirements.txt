# libsql-experimental is Turso's Python client — used when TURSO_DATABASE_URL is set,
# to persist data across ephemeral Render deploys. Falls back to local sqlite3 otherwise.
libsql-experimental>=0.0.41
